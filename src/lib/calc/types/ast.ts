/**
 * Syntax tree traverse order enumeration
 * @enum
 */
export const enum E_SYNTAX_TREE_TRAVERSE_ORDER {
  /**
   * Pre-order traverse order
   */
  PRE_ORDER = 'PRE',
  /**
   * In-order traverse order
   */
  IN_ORDER = 'IN',
  /**
   * Post-order traverse order
   */
  POST_ORDER = 'POST',
}
